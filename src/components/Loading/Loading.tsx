import React from "react";
import "./Loading.css";

const Loading = () => {
  return (
    <div className="loadingContainer">
      <span>Loading...</span>
    </div>
  );
};

export default Loading;
